# 実装状況 / 残作業

最終更新: 2026-08-14

> 過去の修正の詳細な経緯は git のコミット履歴を参照（このファイルは「現状サマリ」に整理済み）。
> 設計の正本は [SPEC.md](SPEC.md)、全体像と運用の落とし穴は [HANDOFF.md](HANDOFF.md)。

## 起動 / 確認
```
docker compose up -d --build
```
- フロント: http://localhost:5173 （admin@demo.local / demo1234）
- API(OpenAPI): http://localhost:8100/docs
- ※ Windows×Docker ではコードが自動反映されないことがある。`docker compose restart backend|frontend` ＋ ブラウザ Ctrl+Shift+R（詳細は HANDOFF §3）。

## 実装済み・検証済み
- **取り込みで行が勝手に1行にまとまらない**: 1列目を無条件に ID として扱っていたため、そこが被る表（工程名・顧客名など、ID ではない列が先頭にある表）を入れると、別々の行が黙って1行に潰れていた。取り込みウィザードに **「行の照合」** を足し、既定を **「照合しない（Excelの1行＝アプリの1行）」** にした。選べるのは3つ — ①**照合しない**（既定。IDが被っても別の行。ID列を指定しなければ**内部のIDを自動採番**するので、IDの無い表もそのまま入る）②**IDで照合して更新**（従来の動き。同じIDは1行にまとまる）③**入れ替え**（取り込む前にそのシートの行を全部消してから入れる＝何度取り込んでも中身が増えない）。照合を切り替えると ID 列の指定も追従する（照合しない→自動採番、IDで照合→先頭列）。プレビューの件数・警告も選んだ照合どおりに出る（入れ替えなら「削除 N 行」）。設定は**プリセットに記録**されるので、一括取り込みでも同じ照合で再現される。**一括取り込みの既定は従来どおり「IDで照合」**（同じファイルを繰り返し流す場所なので、既定を変えると取り込み直すたびに行が倍になる）だが、一覧に「行の照合」の列が出るのでワークシートごとに切り替えられる。API に `match_mode` を送らない呼び出しは従来のまま。
- **データのお掃除（グループ管理・管理者）**: 画面から行やシートを消しても、消えないものが5種類あった — 変更履歴・週次スナップショット・既読の通知・**消した列の値**（`rows.data` に列IDのまま残り、行を開いても出てこない完全に見えないデータ）・予定も実績も無い工数セル。加えてバックアップは1件がグループ全体の JSON なので、いちばん容量を食う。「データのお掃除」で**いまの中身（テーブルごとの件数とサイズ、DB全体のサイズ）**を見て、消すものを選び、**先に「消える件数」を出してから**削除できる。履歴とスナップショットは日数・週数で線を引ける（既定は消さない側にチェックが入っていない）。実行は1トランザクション。`GET /api/maintenance/usage` / `POST /api/maintenance/cleanup`。
- **一括置換（列だけ / シート全体）**: 取り込んだ表記ゆれ（「(株)」→「株式会社」、旧部署名、全角の混入）を直すのに、セルを1つずつ開く必要がなくなった。シート設定の「列」タブの **「⇄ 値の一括置換…」**、または列の設定（一覧の見出し ⋮ → ⚙）から開く。範囲は**シート全体／1つの列／ID だけ**、条件は**部分一致か完全一致**・大文字小文字の区別。**必ず先に「確認する」**で「何セル・何行・どう変わるか（before→after の例つき）」を出してからでないと実行できず、条件を変えたらまた確認からやり直しになる（置換は取り消せないため）。**プルダウンは選択肢そのものも一緒に置換**する（データだけ直すと「選択肢に無い値」が並ぶため）。計算列（参照/数式）は対象外。変更は1セルずつ変更履歴に残る。
- **プルダウンの選択肢を消すとき、その行の行き先を選べる**: 選択肢ごとに**使用件数**が出るようになり、使われている選択肢を消そうとすると「N 行で使われています。その行をどうしますか？」と訊く。**別の選択肢に付け替える／空にする／値は残す**（一覧では「選択肢に未登録」の点線表示になる）から選ぶ。決めた内容は保存前に一覧で確認でき、「保存」を押したときに選択肢の削除と行の書き換えが一緒に適用される（`PATCH /api/columns/{id}` の `value_remap`）。
- **Excel一括取り込みは、入力のたびに読み込み直さない**: 見出し行を1文字打つ・取り込み先を選ぶ、そのたびにブック全体を読み直して画面が「読み込み中…」に切り替わっていた。再解析は **「確認しなおす」ボタンを押したときだけ**にし、変更があるのに確認していない間は「変更しました。件数は『確認しなおす』を押すと更新されます」と出して実行ボタンを止める（古い件数のまま流さないため）。取り込みウィザードの「最終行」も、打っている途中では解析せず **Enter か入力欄から離れたとき**に反映する（プレビューの ✂ は従来どおり即時）。
- **バージョンが画面で分かる**: サイドバー下部に `v1.1.0 (コミット)`。クリックすると **画面（ブラウザが読んでいる JS）** と **サーバ（API コンテナ）** のバージョン・コミット・ビルド日時が並び、**食い違っていれば警告**が出る（「直したはずなのに直っていない」の切り分けは、まずここ）。ログイン画面にも版が出る。値は `docker build --build-arg APP_COMMIT=$(git rev-parse --short HEAD) --build-arg APP_BUILT_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)` で埋める（渡さなくてもビルドは通り、画面には「開発」と出る）。サーバ側は `GET /api/version`（認証不要）。
- **一覧の表示が重くない（400行クラス）**: テーブル型シートは全行を DOM に出していた（323行×32列＝**33,000ノード**）ため、検索を1文字打つ・並べ替える・セルを保存する、そのたびに **600〜680ms** 画面が固まっていた。**行を仮想化**して見えているぶんだけ描くようにし、DOM は約 3,000 ノード、再描画は **0〜1ms** になった。あわせて **参照(LOOKUP)の解決を索引化**（それまでは1セルごとに参照先を線形探索していて、400行×400行のマスタで1列あたり16万回の比較。しかも列幅の計測・絞り込みの候補作り・検索・セル描画で同じ計算を何度も通っていた）し、**同じ描画内で解決済みの値を使い回す**ようにした。
- **Excel の数式を数式列として取り込む**: これまでは計算結果だけが入り、元の列を直しても動かない“焼き付いた数字”になっていた。取り込みウィザードが Excel の数式を読み、**同じ行の・取り込む列だけ**を指している式を `[列名]` 形式に翻訳して**数式列として提案**する（`=C2*D2` → `[単価]*[数量]`、`=ROUND(E2*1.1,0)`、`=G2-TODAY()`、`=IF(D2>5,"大口","")` など。同じ行の横方向の範囲 `SUM(C2:F2)` は個別引数に展開）。**他の行・他シート・列全体の参照、未対応の関数、行ごとに式が違う列は翻訳しない** — その場合は理由を画面に出したうえで、従来どおり計算結果を値として取り込む。列ごとに「数式のまま／値として」を選び直せる。
- **XLOOKUP を参照(LOOKUP)列として取り込む／テーブルの数式が読める**: Excel の `XLOOKUP` / `VLOOKUP`（**完全一致のものだけ**）を読み、「このシートの◯◯列で、あのシートを引いて△△を表示する」という**参照列として提案**する（`=XLOOKUP([@品番], マスタ[品番], マスタ[品名])` → 「マスタ」を品番で引いて品名を表示）。結びつけ先は **①そのワークシートを取り込んだ先のシート（取り込み設定の記録）→ ②同じ名前のシート** の順に探し、マスタ側で **ID列にした列**（行の key_value になっていて列として存在しない）も照合先として拾う。`IFERROR(XLOOKUP(…),"")` は中身だけ見る。**参照先のシートがまだ無い／列が見つからない場合は参照列にしない** — 理由を画面に出したうえで、従来どおり計算結果を値として取り込む（作っても永久に空欄になるため）。あわせて **Excel の「テーブル」機能で書かれた数式**（構造化参照 `=[@数量]*[@単価]`、`=テーブル1[[#This Row],[数量]]`）も翻訳できるようになった — これまではテーブルのブックは数式が1つも読めていなかった。テーブル定義は .xlsx の zip から直接読む（openpyxl は read_only ではテーブルを持ってこないため）。
- **まとめて選択して削除**: テーブル型シートの行にチェックボックス（**Shift+クリックで範囲選択**、見出しのチェックで**表示中の行を全選択**）。選ぶと画面下に「N 行を選択中／削除」が出る。削除は `POST /api/rows/bulk-delete` の**1トランザクション**（全部消えるか、1つも消えないか）で、変更履歴にも1行ずつ残る。
- **行の右クリックメニュー**: テーブル（開く／選択／複製／削除）とスケジュール（開く／フェーズ◇／先行タスク／子タスク追加／削除）の両方。選択が複数あるときは「選択した N 行を削除」になる。
- **列の設定を一覧の上で開ける**: 見出しの ⋮ に「**⚙ この列の設定…**」。列名・型・選択肢・ステータスルール・参照(LOOKUP)・数式を**その場のモーダル**で直せる（シート設定へ行って戻る往復が要らない）。中身はシート設定と同じ部品。

- **列見出しの絞り込みは OK で確定**: チェックを変えた瞬間ではなく、メニュー下部の **OK** を押したときに反映（キャンセル／クリアも同じ場所）。メニュー内でホイールを回しても閉じず、一覧はメニュー内でスクロールする。検索欄に文字を入れると**一致するものだけにチェックが絞られる**（Excel と同じ）。
- **並べ替えがフィルタで消えない**: 並べ替えの状態を SchedulePage 側で持ち、シートごとに永続化。絞り込みが 0 件になってグリッドが一時的に消えても並びは維持される。
- **既定の表示（絞り込み・並べ替え）**: ツールバーの「既定に戻す」で保存済みの既定へ復帰（未保存ならすべて解除）。▾ から「今の表示を既定にする」「既定を削除」。シートごと・ブラウザごとに保存。
- **工数は整数表示**: 週セル／予定計・実績計・差／ダッシュボード／マイタスクは小数を出さない（保存値の精度はそのまま。正確な値はツールチップに表示）。
- **長い列名が見える**: 列幅に収まらない見出しは1行で切られ、しかもマウスを当てても出るのは「クリックで並べ替え」の説明だけで、**何の列か確かめる手段がなかった**。見出しは**2行まで折り返す**ようにし（3行目以降は …。見出しは固定表示なので、伸ばし放題にすると見える行が減る）、**ツールチップの1行目に列名そのもの**を出すようにした。⋮ の絞り込みメニューの見出しも折り返す（ここは列幅に縛られないので全部読める）。スケジュール／テーブルの両方。
- **列幅**: テーブル型シートも中身に合わせた幅になり、見出しの右端ドラッグで変更・固定できる（ダブルクリックで自動幅へ）。幅はスケジュールと共通で列 ID 単位に保存。
- **Excel取込はすべてプレビュー付き**: 3か所（シート追加／既存シートへの取込／日報の取込）とも同じ3ステップのウィザードで、**書き込みは最後の1回だけ**。①ワークシートと見出し行（行番号クリックで変更・自動判定つき）②列の対応（既存シートは「Excelの列→シートの列」、日報は「項目→Excelの列」を選択）③プレビューと件数（既存シートは**新規/更新の行数**、日報は**追加/重複スキップ/取り込めない件数と行ごとの理由**）。件数はサーバ側で本番と同じ処理を空実行して算出するため、プレビューと実際の結果が一致します。
- **シートを Excel から新規作成（取り込みウィザード）**: 「シート追加」で .xlsx を選ぶと、いきなり取り込まずに**3ステップで確認**してから作成する。①どのワークシート／何行目が見出し（行番号クリックで変更・既定は自動判定）／どの列が ID（自動採番も可）、②取り込む列のチェック・**列名と型の変更**（日付／数値／メンバー／プルダウン／自由入力）、③**プレビューと警告**（型に合わない値の件数と例・IDの重複や空・列名重複）。週列・進捗・先行タスクは従来どおりの意味で扱う（`POST /api/sheets/import.xlsx/inspect` で解析 → `POST /api/sheets/import.xlsx` で確定）。
- **Excel一括取り込み（設定を記憶して2回目以降を1発に）**: 取り込みが成功すると、その設定（ワークシート／見出し行／ID列／列の対応／取り込み先シート）が**ワークシート名をキーに自動保存**される（組織で共有）。ユーザーは何もしなくてよく、最初にウィザードで丁寧に確認した作業がそのまま設定登録になる。左メニューの **「Excel一括取り込み」** に .xlsx を1つ渡すと、ブック内の全ワークシートが「取り込み先／見出し行／新規・更新の行数／確認事項」の一覧になり、行ごとに取り込み先の変更・新規シート作成・除外ができる。実行は**1トランザクション**で、どれか1つでも失敗したら**全部取り消し**（半分だけ入った状態にならない）。設定は「実際に入ったシート」を指す形で保存し直されるので、**初回=新規作成 → 次回以降=ID照合で更新**になり、何度取り込み直してもシートも行も増殖しない。**設定の無いワークシートは既定で「取り込まない」**（ブックを渡しただけでシートが量産されない）。既存シートへの単発取込でも、前回の設定があれば自動で読み込まれる（`/api/import/presets`、`/api/import/workbook{,/inspect}`）。
- **取り込む範囲を末尾で切れる（これ以降を取り込まない）**: Excel の一番下には合計行・注記・別表など、表ではない行が入っていることが多い。取り込みウィザードの1ステップ目に**末尾のプレビュー**（先頭30行とは別に、既定で最後の15行）が出て、各行の **✂ を押すとその行から下が除外**される（✂ は先頭側のプレビューにも全行に付いている）。切る位置がもっと上のときは、**「↑ さらに上を表示（50行）」で遡る**か、**「最終行」に行番号を直接入力**すれば**その前後が自動で表示**されるので、遠い行でも目視して確定できる。除外される行は取り消し線で表示され、「取り込むのは N 行（それ以降の M 行は取り込みません）」と常に出るので、切りすぎ・切り足りないがその場で分かる。空行を詰める前の**物理行番号**に対して効くので、プレビューでクリックした番号がそのまま効く。この設定は3つのウィザード（シート追加／既存シートへの取込／日報の取込）すべてと**一括取り込み**にあり、**プリセットにも記録される**ので2回目以降は自動で同じところで切れる。
- **再取り込みは「触る列」だけを上書きする**: 取り込みはマッピングで指定した列にしか書かない。**参照(LOOKUP)列は常に無視**（計算値なので、取り込んだ後で列をLOOKUPに変えても再取り込みで値が書き戻ることはない）。マッピングに無い列・取り込み後に手で追加した列・手で直した値もそのまま残る。判定は**取り込み実行時点の列定義**に対して行うので、古いプリセットが今はLOOKUPになった列を指していても安全。ただし列を**改名・削除**するとプリセット（列名で保存）はその列を見失う。無視されるので壊れはしないが、件数が黙って減らないよう、プレビューと一括取り込みの確認事項に**「取り込み先が見つかりません」「参照(LOOKUP)列のため取り込みません」と理由付きで表示**する。
- **プルダウンは「追加できない」「取り込むと消える」を直した**: 3つ別々の不具合が重なっていた。①**選択肢の追加ボタンが無反応**だった — 選択肢のIDを `crypto.randomUUID()` で作っていたが、これは https / localhost にしか生えない API で、社内配布の `http://<サーバーIP>:8080` では `undefined`。例外で黙って死んでいた（フォールバックを実装）。②**選択肢に無い値がセルで「—」になっていた** — 取り込みは `row.data` に直接書くので、値は入っているのに一覧では消えたように見えた（点線付きで値を表示し、選び直せるようにした）。③**取り込みが選択肢を育てていなかった** — 取り込んだ値をそのまま選択肢に追加するようにし（1列 60 種類まで）、超える列は「追加していません」と理由を出す（住所のような自由記述が数百件の選択肢になるのを防ぐため）。シート設定／列の設定には「**選択肢に無い値が N 種類あります → すべてを選択肢に追加**」が出る。「追加」しただけでは保存されないので、**未保存の警告**も出すようにした。
- **見出しの役割はスケジュール／テーブル共通**: 両方とも、**タイトルをクリック＝並べ替え**（昇順→降順→解除）、**右の ⋮ をクリック＝絞り込み**。スケジュールも同じ操作に統一した（従来は見出しクリックでメニューが開いていた）。ID列・集計列（予定計/実績計/差/進捗）は絞り込みが無いので、タイトルクリックだけで並べ替わる。絞り込みはスケジュールと同じ仕組み（値のチェックリスト／日付の年月日ツリー／数値の条件、OK で確定）で、条件はシートごとにブラウザへ保存される。絞り込み中は見出しに ⏷、右上に「絞り込み N/M ×」が出て、ワンクリックで全解除できる。
- **列幅は本当に縮められる**: ①ID（鍵）列だけ固定幅＋ドラッグ枠なしだったので、共有の幅設定（`__id__` キー）に載せてスケジュール・テーブル両方でリサイズ可に。②テーブルは `table-fixed` なのに幅が `max-content` だったため、ブラウザが `<colgroup>` ではなく中身から列幅を決めてしまい、特定の列が中身より細くならなかった → **列幅の合計を明示的な `width` として渡す**ように修正。③セル内の `<select>` / `<input>` は固有の最小幅を持ち `w-full` では潰れないので `min-w-0` を追加、`<td>` に `overflow-hidden` を付けて隣の列へはみ出さないようにした。
- **「開く」はスケジュールにもあり、アイコン表示**: レコード詳細（全項目の編集＋**そのタスクの変更履歴**）を共通部品 `components/RecordModal.tsx` に切り出し、テーブルとスケジュールの両方から開けるようにした。ボタンは行ホバーで浮かび上がる小さなアイコン（枠付き「開く」は全行に並ぶと目立ちすぎるため）。スケジュールでは ID セルの ◇ の左に置いている。**レコード詳細は項目数で段組みが変わる**（9項目以上で2列・幅820px、それ未満は従来の1列）。自由入力（複数行になりうる列）は2列時も横幅いっぱいを使う。
- **バックアップ / 復元（グループ管理）**: グループの**すべて**（シート・列・タスク・週次工数・マイルストン・日報・変更履歴・メンバー・各種設定）を1件のバックアップとして保存し、ワンクリックでその時点に**完全復元**できる。.json でダウンロードしておけば DB ごと失っても戻せる。要点は **IDを保ったまま書き戻す**こと — `rows.data` は列IDがキー、参照(LOOKUP)列やステータスルール・完了条件・実績入力の表示列も列IDを持つので、採番し直すと「シートは開くが別の列を見ている」という静かな壊れ方をする。復元は1トランザクションで、**実行前に「復元前の自動バックアップ」を自動取得**するので戻しすぎても戻せる。復元後は id シーケンスを張り直す（しないと次の追加が重複キーで落ちる）。通知は対象外（再生成される）。**同じグループにしか復元できない**／**管理者を含まないバックアップは拒否**する。メンバーも復元対象なので、バックアップ後に追加したメンバーは消え、自分のアカウントが無い場合はログアウトされる（画面で明示）。
- **マイタスクの担当者切替**: 自分以外のメンバー、または全メンバーのタスクを表示できる（選択はブラウザに保存）。
- **マイタスクの絞り込み・並べ替え**: 「完了を隠す」「年度（4月〜翌3月）」「ステータス」で絞り込み、ID/件名/終了週/予定計/ステータスで並べ替え（見出しクリックでも切替）。**選んだ表示はブラウザに保存**され、次に開いたときも同じ状態から始まる。完了の判定はスケジュール画面と同じ（シート設定の「完了とみなす条件」→ なければステータス列＝完了）。
- **実績入力の分類は段数も可変**: 「分類の設定」（実績入力の右上、管理者）で **段（大分類・中分類・小分類…最大3段）の追加・削除・改名**と、各段の項目の追加・削除ができる。段の構成は実績入力・みんなの入力一覧・Excel入出力すべてに反映（保存は `work_logs.cat1..cat3`、名称は `org.settings.worklog.category_levels`）。
- **タスクの表示列を選べる**: シート設定の「実績入力でのタスク表示」で、ID・件名だけでなく任意の列（顧客・工程など）を選んで「/」区切りで表示。実績入力のタスク選択と**みんなの入力一覧のタスク欄**の両方に反映（シートごとの設定）。
- **年間開発計画**: 大／中／小分類に**参照(LOOKUP)列**も選べる（他シートから解決した表示値で集約）。分類の組み合わせは**名前を付けて保存**でき（シートごと）、ワンクリックで切替。前回の選択も自動で復元。
- **ガントの◇は小さめ**: マイルストンの◇／◆を従来の約半分（9px→5px）に縮小し、週セルの数字を邪魔しない。

- **グリッド操作（Excel 相当）**: 週セルを**ドラッグで範囲選択**（Shift+矢印で拡張）、**Ctrl+C / Ctrl+V** で TSV コピー・貼り付け（Excel と直接やり取り可）、**Delete で一括クリア**、非編集時の**矢印キー移動**、数字を打つと即入力開始。範囲書き込みは `PUT /api/effort/bulk` の1リクエストにまとめる。親タスクのセルは親＋子の合算表示なので、同じ操作で子も書き換わる場合は差分を相殺して**入力した数字どおりに着地**する。
- **元に戻す / やり直す**: **Ctrl+Z / Ctrl+Y**（ツールバーの ↶ ↷ でも可）。工数・属性セル・進捗・ID の編集が対象。取り消しは記録した「前の値」を*保存値の単位*で持ち、実行時に行の最新 version を引き直すため、時間が空いても衝突しない。シートを移ると履歴はクリア。
- **変更履歴**: `row_events` テーブルに編集を1件ずつ記録し、シート単位（ツールバー「変更履歴」）とタスク単位（テーブルのレコード詳細）で「誰が・いつ・何を」を参照できる。列名や値は記録時点の表示文字列で保存するため、列を改名・削除しても過去の履歴が読める。
- **横断検索**: サイドバーの「タスクを検索」または **Ctrl+K**。全シートのタスクID・件名・属性値を横断検索し、選ぶと該当シートの該当行までスクロールして光る。検索はDB側で実行（全行のダウンロード不要）。
- **週列の仮想化**: 表示範囲の週だけを描画（実測で全152週中21週＝約1/7のDOM）。時間軸を広げても描画量が増えない。
- **認証 / グループ管理**: Cookie セッション認証。ログインIDはメール形式でなくても可（`@` 不要）。ログイン画面から**新しいグループ（組織）を作成**できる（`/api/org/signup`）。「グループ管理」画面で、管理者はメンバーの追加・**ロール変更**・**削除**（自分自身は除く）に加え、**グループ名**と**アプリ表示名（サイドバー上部・グループごと）**を編集できる。
- **Excel 入出力**: スケジュール／テーブルは**シート設定**画面から **.xlsx 出力＆取込**（属性列＋週次工数＋**進捗・先行タスク・テンプレ◇の予定/実績列**。開始日/完了日は実列として出力。取込は **ID で upsert**、参照(LOOKUP)列は無視、先行タスクはIDで復元）。**みんなの入力一覧**も日報の出力／取込（取込は管理者のみ）。エクスポート→全削除→取込で進捗・依存・◇まで復元可。
- **データ全削除（設定保持）**: シート設定の「データ管理」から**このシートのデータを空にする**（行・工数・◇・スナップショットを削除、列/設定は保持、採番1にリセット）。グループ管理から**全シートのデータを空にする**（管理者・インポートのやり直し用）。
- **開始日／完了日の列**: `date`型の実列（`config.sched_role`）として自動生成。列一覧で並べ替え・固定・ソート可。直接編集すると割合に応じて◇日付を自動再配分。旧 `__sched_*` 値は初回アクセス時に移行。
- **マイルストン達成**: 達成チェックは廃止し、**実績日を入れると達成**扱い（[MilestoneEditor]）。
- **テーブルのレコード編集**: テーブルシートの行を「開く」で**モーダル全項目編集・参照**（一覧のインライン編集に加えて）。
- **完全オフライン動作**: 運用中はアプリが社外ネットワークへ一切アクセスしない（画面フォントを端末標準にフォールバックし Google Fonts CDN を撤去）。導入/更新時のみ部品取得にネットが必要（社外不可なら持ち込みイメージ方式）。
- **シート**: CRUD（サイドバー一覧＋追加、シート設定から削除＋インライン改名）、`/sheets/:id` ルーティング。スケジュール型 / テーブル型。シート設定はシート種別に応じて表示（テーブルは固定列・絞り込み・色基準・既定マイルストン等のスケジュール専用項目を非表示）。テーブルは各行を**モーダルで全項目編集**（「開く」）。
- **スケジュール（週次グリッド）**: 週セル直接入力（過去=実績/今日以降=予定）、連続ガント＋◇マイルストン（名称・色・達成done）、今日線、年バンド（約3年）、左端固定列数の設定（通常/最小の2段階）、列並べ替え・ソート、月/週トグル。
- **変化点ハイライト**: 当週スナップショット（断面）と現在値を比較し、変えた週/セルを赤表示（工数＋属性列）。
- **ステータス**: 手動ドロップダウン編集／`auto_from_milestones` でマイルストン達成から自動判定。
- **lookup(XLOOKUP)**: ID/列で他シート照合（先頭一致）。行ID(key_value)編集可、同一ID重複可。
- **その他画面**: テーブル型シート、ダッシュボード（簡易ピボット＋バー）、マイタスク、年間計画、日報（実績入力・モバイル対応・みんなの入力一覧）、シート設定（列CRUD＋プルダウン＋ステータスルール＋lookup設定）。
- **ダッシュボード / マイタスクのシート選択**: 先頭シート固定をやめ、シート切替を追加（選択は画面ごとに永続化）。マイタスクのIDからスケジュールの該当行へ飛べる。
- **シート設定の整理**: 「列 / 表示 / 入出力 / 危険な操作」の4タブに分割。列一覧は**ドラッグで並べ替え**（▲▼ボタンも維持）。
- **ステータスルールのプレビュー**: 設定中のルールを実データに当てて「各ルールに何件該当するか」「どのルールにも当てはまらない行が何件か」「上のルールが常に一致して届かないルール」を表示。
- **UIの体感**: 読み込み中はスケルトン表示、データが無い画面には次の一歩を示す空状態、編集の保存状況を「保存中… / ✓ 保存しました」で表示。
- **通知（ベル）**: behind / 逆ザヤ / 超過 / マイルストン / 日報未入力 のアプリ内通知（dedupe で冪等）。
- **基盤**: Alembic マイグレーション整備済み（起動時に既存DBを baseline へ stamp → upgrade head）。CSV エクスポート。失敗時トースト。pytest / vitest。日次バックアップ（`--profile backup`）。
- **デプロイ**: 本番（GitHub Actions → GHCR → EC2/Compose、host nginx で TLS 終端）と、GitHub を使わないオフライン build 構成（[オフライン導入手順.md](オフライン導入手順.md)）。

## 残課題 / 既知の制限
- スケジュール型シートの行には**チェックボックスによる複数選択が無い**（右クリックからの1行削除のみ）。週セルの範囲選択と操作が競合するため、入れるなら選択モードの切り替えが要る。
- 参照(LOOKUP)の自動生成は**新規シート作成のウィザードだけ**。**一括取り込み（ブック丸ごと）は対象外** — 同じ実行の中でマスタと明細を同時に作ると、明細を作る時点でマスタがまだ無いことがあり、シートを全部作ってから参照を張り直す二段構えが要る。回避策は「先にマスタのワークシートを取り込む → 明細を取り込む」の順で流すこと。
- 参照(LOOKUP)にできる LOOKUP は **XLOOKUP / VLOOKUP の完全一致だけ**。`INDEX+MATCH`、近似一致、複数条件、`XLOOKUP` の末尾から検索（第6引数 -1/-2）は対象外（理由を出して値として取り込む）。
- 数式の翻訳は**取り込み時の1回だけ**。取り込んだ後で Excel 側の式を変えても追従しないし、翻訳できなかった式を後から直す導線は「列の設定 → 数式」を手で書く形になる。
- `_MAX_AUTO_OPTIONS`（1列 60）を超えるプルダウン列は、取り込みでは選択肢が増えない（理由は画面に出る）。「すべてを選択肢に追加」は列の設定から手動で実行できる。
- 週セルのグリッド色（変化点ハイライト）は従来どおりスナップショット差分ベース。行単位の詳しい経緯は「変更履歴」を参照する二本立て。
- 範囲貼り付け（`/api/effort/bulk`）は last-write-wins で、単セル編集のような version 衝突検出はしない（履歴には残る）。
- 月表示での範囲編集は、入力した月合計をその月の各週へ均等分割する。元に戻すと分割後の状態ではなく「前の月合計を均等分割した形」に戻るため、週ごとの内訳は厳密には復元されない。
- lookup の循環参照ガード無し。
- ダッシュボード集計は基本のみ（多軸ピボット・シート横断の集計は未）。エクスポートは CSV / Excel。
- 変更履歴・週次スナップショットの**自動**削除は無い（「データのお掃除」から手で消す。放っておけば増え続けるのは変わらない）。
- 一括置換は**元に戻せない**（変更履歴には1セルずつ残るが、まとめて戻す操作は無い）。実行前の確認と、必要ならバックアップで担保している。
- 「入れ替え」で取り込むと、そのシートの**工数・◇・進捗も消える**（行ごと消してから入れ直すため）。アプリ側で育てた情報があるシートには向かない。

## 次の一手の候補（UX 全面検証で残した宿題 / 効果が大きい順）
1. **行削除の取り消し**: いま Ctrl+Z が効くのは工数・セル・進捗・IDだけで、**行の削除は戻せない**（まとめて削除を入れたぶん、取り違えたときの痛みが大きい）。削除済み行を一定期間持つか、削除前スナップショットを積む。
2. **テーブル型シートにも範囲選択・コピー／貼り付け**: スケジュールの週セルには Excel 相当の操作（ドラッグ選択・Ctrl+C/V・Delete）があるが、テーブル型シートには無く、属性セルは1つずつしか触れない。「Excel と比べて面倒」の残りはほぼここ。
3. **参照(LOOKUP)の設定が分かりにくい**: いまは「参照先シート／照合する列／返す列」を別々のプルダウンで選ぶ形で、選び終わるまで結果が見えない。**選びながら実データで1件プレビュー**すれば、当たっているかがその場で分かる。
4. 新規行の入る位置（いまは常に末尾。Excel の「上に行を挿入」に相当する操作が無い — 並び順を持たせるか、挿入時に採番を割り込ませるかの判断が要る）。
5. 変更履歴の保持期間の**自動**適用（いまは「データのお掃除」から手で実行）と、シート横断の変更フィード。
6. ダッシュボードのシート横断集計・多軸ピボット。
7. 範囲貼り付けの衝突検出（他の人が同じ範囲を触っていたら警告する）。
