# オフライン導入手順（GitHubを使わないUbuntuサーバーで動かす）

GitHub につながらないサーバーでも、このアプリを動かせます。
やることは「ファイルをコピー → 起動コマンドを打つ」だけです。難しい知識はいりません。

このアプリは **Docker（ドッカー）** という仕組みで動きます。Docker は「アプリと必要なものを箱詰めにして、どのサーバーでも同じように動かす道具」だと思ってください。

---

## 0. 前提

- Ubuntu のサーバー（22.04 以降を推奨）
- サーバーに管理者として入れること（`sudo` が使えること）
- ファイルをサーバーに運ぶ手段（USBメモリ、社内ネットワーク、`scp` など）

サーバーが **Docker Hub（ドッカーの公式置き場）には繋がる** 場合 → この手順そのままでOK。
**Docker Hub にも一切繋がらない完全オフライン** の場合 → 最後の「補足B」を見てください。

---

## 1. Docker を入れる（最初の1回だけ）

サーバーで次のコマンドを順番に実行します（コピーして貼り付けでOK）。

```bash
sudo apt update
sudo apt install -y docker.io docker-compose-v2
sudo systemctl enable --now docker
# 自分のユーザーでも docker を使えるようにする（このあと一度ログインし直す）
sudo usermod -aG docker $USER
```

いったんログアウト → 再ログインしてから、次で確認します。

```bash
docker version
docker compose version
```

バージョンが表示されれば準備OKです。

---

## 2. アプリのファイルをサーバーに置く

このプロジェクトの **フォルダ一式** をサーバーにコピーします。
（`backend` `frontend` `docker-compose.offline.yml` などが入っているフォルダごと）

置き場所はどこでも構いませんが、例として `/opt/schedule` にします。

```bash
sudo mkdir -p /opt/schedule
# USBやscpで運んだフォルダの中身を /opt/schedule に入れる
# 例（手元PCから送る場合）:  scp -r ./2606_schedule/* ユーザー名@サーバーIP:/opt/schedule/
sudo chown -R $USER:$USER /opt/schedule
cd /opt/schedule
```

> GitHub は使いません。`git` も不要です。フォルダの中身さえあれば動きます。

---

## 3. 設定ファイル（.env）を作る

`/opt/schedule` の中で、見本をコピーして編集します。

```bash
cp .env.prod.example .env
nano .env      # nano という簡単なエディタで開きます（保存=Ctrl+O→Enter、終了=Ctrl+X）
```

最低限、次の項目を書き換えてください。

```ini
# データベースのパスワード（推測されにくい文字列に）
POSTGRES_PASSWORD=好きな強いパスワード

# ログインのための秘密の文字列（長いランダム文字列に）
SESSION_SECRET=長いランダムな文字列

# http（鍵マークなし）で開くなら false にする。←重要
COOKIE_SECURE=false

# 最初の管理者（初回起動時に自動で作られる）
DEMO_ADMIN_EMAIL=あなたのメール
DEMO_ADMIN_PASSWORD=最初のログインパスワード
```

ポイント：
- **`COOKIE_SECURE=false` を忘れずに。** `http://（IPアドレス）:8080` のように鍵マークなしで開く場合、`true` のままだとログインできません。
- `BACKEND_IMAGE` / `FRONTEND_IMAGE` の行は、このオフライン手順では **使いません**（書いてあっても無視されます）。

---

## 4. 起動する

```bash
cd /opt/schedule
docker compose -f docker-compose.offline.yml up -d --build
```

- `--build` … サーバー上でアプリを組み立てます（初回は5〜10分ほどかかります）。
- `-d` … 裏側で動かし続けます。

うまくいったか確認：

```bash
docker compose -f docker-compose.offline.yml ps
```

`backend` `frontend` `db` が **running（動作中）** になっていればOKです。

---

## 5. 画面を開く

同じ社内ネットワークのパソコンのブラウザで開きます。

```
http://<サーバーのIPアドレス>:8080
```

例：サーバーのIPが `192.168.0.50` なら `http://192.168.0.50:8080`。

`.env` に書いた管理者メール・パスワードでログインできます。
**ログイン後、必ずパスワードを変更してください。**

> 初回ログインが終わったら、`.env` の `SEED_ON_STARTUP=false` に変えて、もう一度「6. 更新する」の手順で再起動しておくと安全です（自動で初期データを作り直さなくなります）。

---

## 6. アプリを新しくする（更新する）

新しいバージョンのフォルダを受け取ったら：

1. `/opt/schedule` の中身を **新しいファイルで上書きコピー**（`.env` と `db` の中身はそのまま）
2. 次を実行：

```bash
cd /opt/schedule
docker compose -f docker-compose.offline.yml up -d --build
```

これで新しい画面に入れ替わります。
データベースの中身（入力したデータ）はそのまま残り、必要な更新（マイグレーション）は **起動時に自動** で行われます。

---

## 7. 停止・再起動・ログの見方

```bash
# 停止（データは消えません）
docker compose -f docker-compose.offline.yml stop

# もう一度動かす
docker compose -f docker-compose.offline.yml start

# 完全に止めてコンテナを片付ける（データは消えません）
docker compose -f docker-compose.offline.yml down

# 何か変なときはログを見る
docker compose -f docker-compose.offline.yml logs -f backend
```

サーバーを再起動しても、Docker が入っていれば自動で立ち上がります（`restart: unless-stopped` の設定済み）。

---

## 8. データのバックアップ（大事）

入力データは `db`（データベース）に入っています。定期的に書き出しておくと安心です。

```bash
# バックアップを取る（schedule.sql というファイルに保存）
docker compose -f docker-compose.offline.yml exec -T db pg_dump -U app schedule > schedule_$(date +%Y%m%d).sql
```

戻す（復元）とき：

```bash
cat schedule_20260101.sql | docker compose -f docker-compose.offline.yml exec -T db psql -U app -d schedule
```

> `app` はユーザー名です。`.env` の `POSTGRES_USER` を変えた場合はそれに合わせてください。
> `schedule_*.sql` のファイルは USB などに別途保管しておくと、より安全です。

---

## 補足A. ポートを変えたい / 80番で開きたい

`docker-compose.offline.yml` の `frontend` の `ports` を変えます。

```yaml
    ports:
      - "8080:80"     # 左の 8080 を好きな番号に。例: "80:80" にすると http://<IP> だけで開ける
```

`80` を使うと `http://<サーバーのIP>` だけでアクセスできます（番号が要らなくなる）。
変更後は `docker compose -f docker-compose.offline.yml up -d` を実行。

---

## 補足B. Docker Hub にも繋がらない「完全オフライン」の場合

サーバーがインターネットに一切出られない場合は、**別のインターネットに繋がるPCでイメージを作って、ファイルとして運びます。**

### 繋がるPC側（一度だけ）

```bash
# 1) プロジェクトフォルダで、イメージを組み立てる
docker compose -f docker-compose.offline.yml build

# 2) 必要なイメージを1つのファイルに保存する
docker save \
  schedule-backend schedule-frontend postgres:16-alpine \
  -o schedule-images.tar
```

> イメージ名は `docker images` で確認できます。`schedule-backend` `schedule-frontend` のような名前になります。

### サーバー側（オフライン）

1. `schedule-images.tar` と **プロジェクトフォルダ一式** を USB などで運ぶ
2. イメージを読み込む：

```bash
docker load -i schedule-images.tar
```

3. ビルドせずに起動する（`--build` を付けない）：

```bash
cd /opt/schedule
docker compose -f docker-compose.offline.yml up -d
```

これでインターネットなしでも動きます。
更新時も同じく「繋がるPCで save → 運ぶ → load → 起動」を繰り返します。

---

## 困ったとき

- **ログインできない** → `.env` の `COOKIE_SECURE` が `false` になっているか確認（http の場合）。直したら `docker compose -f docker-compose.offline.yml up -d` で再起動。
- **画面が出ない** → `docker compose -f docker-compose.offline.yml ps` で全部 running か確認。`logs -f backend` でエラーを確認。
- **ポートが使われている** と出る → 補足A で 8080 を別の番号に変える。
- サーバーのファイアウォール（`ufw` 等）を使っている場合は、開けたポート（例 8080）を許可してください：`sudo ufw allow 8080`。
