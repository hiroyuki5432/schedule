# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
dist-types/
.vite/
*.local

# Claude Code local (machine-specific permissions / dev tooling)
.claude/settings.local.json
.claude/launch.json

# Operator-specific docs NOT for the using side (contain personal email /
# GitHub username / PAT steps). Kept locally, not pushed.
# NOTE: .github/workflows IS pushed (needed for CI/CD to actually run) — it only
# references secrets by name, no personal details.
/DEPLOY.md
/OPERATIONS.md

# Env / secrets (real .env is ignored; *.example templates are kept)
.env
.env.*
!.env.example
!.env.prod.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Docker
pgdata/

# Database backups (scripts/backup.sh, db-backup service)
/backups/
