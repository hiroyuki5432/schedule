# CLAUDE.md

Claude Code が毎回読むファイル。**このリポジトリで壊しやすい所だけ**を書く。
全体像と運用の落とし穴は [HANDOFF.md](HANDOFF.md)、機能の現状は [STATUS.md](STATUS.md)、仕様の正本は [SPEC.md](SPEC.md)。

## 1. 何のアプリか

単一組織・小規模チーム向けの「スケジュール＆工数管理」Web アプリ。行＝管理対象（ID=`rows.key_value`）、列はユーザー定義（text / number / date / dropdown / status / member / **lookup / formula**）。
1シートだけ週次グリッド（予定・実績工数、連続ガント、◇マイルストン）、他はテーブル型。
**Excel の .xlsx を取り込んでシートを作る／既存シートを更新する**導線があり、そのなかで **Excel の数式・XLOOKUP を「計算列」として引き継ぐ**部分がいちばん複雑で、いちばん壊れやすい。

## 2. 開発の実務

**ローカルに実行環境は無い。すべて docker 越しに走らせる。** backend に venv は無い。`frontend/node_modules` はホストにも残っているが `.bin` が無いので動かない — 本物はコンテナの中。

```bash
docker compose up -d --build

# backend（テスト用の依存は本番イメージに入っていないので初回だけ入れる）
docker compose exec -T backend pip install -r requirements-dev.txt
docker compose exec -T backend python -m pytest -q

# frontend
docker compose exec -T frontend npm run typecheck   # = tsc -b
docker compose exec -T frontend npm test            # = vitest run
```

- **pytest は並走させない。** `backend/tests/conftest.py` は毎テスト前に共有の `<db>_test` を全テーブル `TRUNCATE ... RESTART IDENTITY CASCADE` する。2本同時に流すとテーブルの排他ロックを取り合ってデッドロックする。1本ずつ。
- `typecheck` は **`tsc -b`**。`tsc --noEmit` は使わない — ルートの `tsconfig.json` は references だけで `files` が空なので、**何も検査せずに成功する**（`frontend/package.json` に注記あり）。本番ビルドだけ落ちる状態を作ってしまう。
- Windows × Docker はコードが自動反映されないことがある。backend を直したら `docker compose restart backend`、ブラウザは Ctrl+Shift+R。「直したのに直っていない」はまずこれを疑う（HANDOFF §3）。サイドバー下部のバージョン表示で画面とサーバの版が食い違っていないかも見られる。

## 3. Excel 取り込みを触るときのチェックリスト

主な場所は `backend/app/xlsx_formula.py`（式・XLOOKUP の翻訳）、`backend/app/routers/excel.py`（列の組み立てとシート作成）、`backend/app/xlsx_import.py`（ブックの読み取り）、`backend/app/xlsx_tables.py`（Excel の「テーブル」定義）、画面は `frontend/src/components/ImportSheetWizard.tsx` と `frontend/src/components/import/`。

この機能で出た不具合は**全部「テストは通る・画面は正常に見える・でも黙って違う結果を出す」**種類だった。以下は「こう壊れたので、こう確かめる」の一覧。

### 1. 式の参照名を「**実際に作られる列名**」で検算する

取り込む直前に `expr_refs(式) ⊆ 作られる列名 ∪ {ID}` を確かめ、外れたら数式列にせず値として取り込む。

> 起きたこと: 「式を作ったときの列名」と「実際に作られる列名」を突き合わせる場所がどこにも無く、①ウィザードで列名を打ち直す ②取り込む列のチェックを外す ③見出しが80字超で切り詰められる ④見出しが重複して列が1本しか作られない ⑤ID列を指している、の5通りで列まるごと `#「◯◯」という列がありません` になった。**5件が同じ根**。

場所: `excel.formula_names_by_index()` / `excel.create_sheet_with_selection()` の `will_exist` / `xlsx_formula.expr_refs()`。

### 2. 「取り込まない列」を参照する式は作らない

列にならないもの（週次工数・進捗・先行タスク・マイルストン、およびチェックを外した列）は `formula_names_by_index()` に載せない。

> 起きたこと: チェックを外した列を参照する式がそのまま残り、列が全行エラーになって、Excel にあった値まで失われた。

### 3. `[ID]` は名前空間が衝突する

ID列は取り込むと「シートの列」ではなく行のキー値になるので、式では `[ID]` に翻訳する。ただし **`ID` という名前の列が実在するなら翻訳せず断る** — `lib/computed.ts` は列名を先に見るので、そちらを読んでしまう。

> 起きたこと: 「ID列を `[ID]` に翻訳する」修正を入れた結果、`ID` という列を持つブックで別の値を黙って読むようになった。**修正が新しいバグを生んだ**。

場所: `xlsx_formula.translate_formula()` の `id_name_taken` / `emit`、`xlsx_formula.ID_REF` ⇔ `frontend/src/lib/computed.ts` の `ID_REF`。

### 4. 式のどの部分も捨てない

使わなかった引数が定数かどうかを必ず確認する。捨てるなら理由を画面に出し、`ready` にしない。

> 起きたこと: `IFERROR(XLOOKUP(…), XLOOKUP(…))` の2つ目を黙って捨てて「きれいに参照列にできました」と表示していた。

場所: `xlsx_formula._strip_wrappers()`（逃げ先が定数のときだけ剥がす）。

### 5. 参照は端まで確かめる — 範囲の行番号とテーブル名

照合範囲と取得範囲の**行がそろっているか**、構造化参照の**テーブル名が自シートか他シートか**を見る。

> 起きたこと: `XLOOKUP(A2, M!$A$2:$A$10, M!$B$3:$B$11)`（1行ずれ）を揃った参照として通した。`XLOOKUP(マスタ[@品番], …)` のキーが自シートの同名列にすり替わった。

場所: `xlsx_formula._same_rows_or_fail()` / `_parse_array()` / `parse_structured_ref()`。

### 6. 式を書き換える処理は、引用符の外だけに効かせる

`_xlfn.` / `@` を外すような書き換えは、文字列リテラルの中に触れてはいけない。**画面に出す「元の Excel 数式」も同じ関数を通る**ので、ここが壊れると利用者に気づく手立てが無くなる。

> 起きたこと: `=B2&"@example.com"` が `[数量]&"example.com"` になり、しかも画面の原文も壊れた姿だった。

場所: `xlsx_formula.strip_excel_internals()`（1文字ずつ `"` で状態を切り替える）。

### 7. 推測で結線しない

参照先の照合列が決まらないなら、行のIDと実際の値が重なるかを確かめる。確かめられなければ**手で選んでもらう**。名前が似ているだけで当てない（間違った参照は空欄より質が悪い）。名前で1件に絞る箇所は、同名が複数あり得るなら**どれを選ぶか順序を決める**か、決められないなら断る。

> 起きたこと: 「マスタの先頭列がID列だろう」と決めつけて行IDに結びつけていた。単発の取り込みは設定を保存しないので、この推測は例外ではなく**常用経路**だった。同名シートがあると `ORDER BY` の無い `.first()` で片方を黙って掴んだ。

場所: `excel._resolve_lookup()` / `excel._column_became_the_id()`、手動指定は `frontend/src/components/import/LookupTargetDialog.tsx`。

### 8. 一部の行だけが数式の列は、既定を数式列にしない

`covers_all_rows` を見て、既定は値のまま。「手で入力されている N 行が計算値に置き換わります」と件数で警告する。

> 起きたこと: 目に見えて入っている手入力値が黙って消えるのが、いちばん気づけない壊れ方だった。

場所: `excel.new_sheet_column_info()` の `partial` / `replaced_values`。**プレビューの表と実際の結果を食い違わせない**（件数はサーバ側の空実行で出す、が全ウィザード共通の約束）。

### 9. 黙って格下げしない・入口ごとに結果を変えない

参照列→値、数式列→値に落とすときは必ず理由を画面に出す。読み替えられた分だけ入口を出す、をやらない（`has_lookup` と `lookup` を分けているのはこのため）。単発ウィザード／既存シートへの再取り込み／一括取り込みの新規・既存で、同じブックが同じ結果になるかを通しで確かめる。

> 起きたこと: 「⚙ 参照先を選ぶ…」が式を分解できた列にだけ出ていて、理由も出ないまま「選べる列と選べない列がある」状態になった。

場所: `excel._column_formula()` の `has_lookup`。※ 参照(LOOKUP)の自動生成が新規シート作成のウィザードだけ、という制限は STATUS.md の「残課題」参照。

### 10. フロントのパーサで往復検証する／いじわるな見出しをサンプルに入れる

バックエンドで式を作って終わりにしない。`frontend/src/lib/formula.ts`（`ARITY`）と `xlsx_formula.SUPPORTED_FUNCTIONS` は**同じ集合**でなければならず、生成した式は `parseFormula` が読めなければならない。サンプルブックには **同名見出し・80字超・空見出し・記号入り（`数量[個]`）・`ID` という名前の列**を必ず入れる。

> 起きたこと: 見出しに `]` が入ると `[数量[個]]*100` という、フロントのトークナイザが読めない式のまま列を作っていた（`[` は最初の `]` で閉じる。エスケープの書き方が無い）。

場所: `xlsx_formula._ref()` が断る。テストは `backend/tests/test_xlsx_formula.py` / `test_xlsx_lookup.py`、フロントは `frontend/src/lib/formula.test.ts` / `computed.test.ts`。

## 4. このコードベース全体で効く原則

上の13件から抜き出したもの。Excel 取り込みに限らない。

1. **利用者が気づけない壊れ方を作らない。** 例外で止まるより、黙って違う値を出すほうが悪い。「テストは緑・画面は正常・結果だけ違う」が出たら、それは最優先で直す種類のバグ。判断に迷ったら「利用者はこれが間違っていると分かるか？」を先に問う。
2. **推測したら、外れたときの文言を推測に合わせる。** 推測で結線してよい場面はある（既定値・自動判定）。だめなのは、推測を確定として見せること。推測なら「◯◯だと判断しました」と書き、確かめられないなら選んでもらう。「参照先が見つかりません」と「XLOOKUP に対応していません」を取り違えると、直しようが無くなる。
3. **作った値が下流で本当に解釈できるか、往復で確かめる。** 生成側と消費側が別の言語・別のプロセスにあるものは、片側だけのテストでは守れない（式の文法、列ID をキーに持つ `rows.data`、バックアップの復元）。片側を直したら、もう片側の対応物を必ず一緒に見る。
4. **入口が複数ある機能は、入口ごとに結果が変わらないことを確かめる。** 取り込みだけで4経路ある。片方だけ直すと「昨日と同じファイルを入れたのに結果が違う」になり、原因の切り分けができない。
5. **修正するときは、その修正が増やす前提を数える。** `[ID]` の一件のように、直した拍子に新しい名前空間・新しい暗黙の了解が1つ増えることがある。増えたなら、それが既存のものと衝突しないかをその場で確かめる。
