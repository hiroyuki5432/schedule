# スケジュール＆工数管理アプリ 仕様書（v1）

> grill-me セッション（2026-06-16）で確定した設計。これが共有理解の基準。
> 「未確定／要確認」の項目は最後にまとめてある。

## 1. 概要

行（管理対象）を任意に定義した列で管理し、週次で予定/実績工数を入力してガント風に可視化する、単一組織・小規模チーム向けの Web アプリ。Excel 的な自由度を持ちつつ DB で一元管理する。

- 行 = 管理対象（例: P26-001）
- 列（横軸）= ユーザー定義（型: 自由入力 / プルダウン / 日付 / 条件付きステータス / メンバー / 参照 / 数値）
- 週次の工数グリッド（予定・実績）をガント風に色付け表示
- 変化点（週次差分）の自動ハイライト
- シート間 XLOOKUP 参照
- ピボット集計ダッシュボード

## 2. 確定事項（決定ログ）

| # | 論点 | 決定 |
|---|------|------|
| 1 | 利用形態 | 単一組織・小規模チーム。全データに `org_id`、マルチテナントの重い仕組みは作らない |
| 2 | スタック | **FastAPI + React(Vite/TS) + PostgreSQL**、本番は docker-compose で Ubuntu、開発は Windows |
| 3 | 可変列の保持 | **JSONB**（`columns` で列定義、`rows.data` に `{col_id: 値}`） |
| 4 | 時間軸の粒度 | **weekly のみ**。開始/終了なしの永続管理。**既定は約1年分を表示**（列幅を細く）。常に週ごとの数値を表示し、値のある週は連続したフェーズ色で塗る。列幅は細/標準/広で調整、表示ウィンドウは自由 |
| 5 | 週の起点 | 月曜始まり ISO 週（組織設定で開始曜日変更可）。`week_start` を正規化 |
| 6 | 工数セルの中身 | **予定 + 実績の両方を保持**。セル表示は単一数値で、**過去週＝実績／現在週以降＝予定**を自動切替。週次スナップショットで過去の計画を as-of 参照 |
| 7 | ガント色（連続バー） | **連続したガントバー**（セル個別枠ではなく繋ぐ）。色は**行ごとのマイルストン＝フェーズ境界日付**で区間ごとに変化、節目を超えて未完なら**遅延色**。判定は条件付きステータス（ルールビルダー）でマイルストン日付を評価 |
| 8 | 変化点 | **前週との差分を自動検知**。対象 = 日付列の値変更 / 行追加 / 列追加・定義変更 / 指定列の値変更（実質、変更履歴） |
| 9 | 変化点の検出方式 | **週次スナップショット**を取り前週と比較。**アクセス時に遅延取得**（cron 不要） |
| 10 | シート間参照 | 各シート独立 + **参照(lookup)列で XLOOKUP**。対象シート/照合キー列/取得列を指定、ライブ計算の読み取り専用、先頭一致、未ヒットは空欄 |
| 11 | 集計 | **専用ダッシュボード（ピボット表 + グラフ）**。スコープは**シート単位**（横断は将来） |
| 12 | 認証 | **メール + パスワード**（管理者がアカウント発行、SMTP 不要）、Cookie セッション、bcrypt |
| 13 | ロール | **Admin / Member の 2 ロール**、全シート共有 |
| 14 | 同時編集 | **楽観的更新 + last-write-wins**（バージョン番号で衝突警告）。WebSocket なし |
| 15 | Excel 連携 | **エクスポートのみ**（CSV/Excel 出力）。インポートは将来 |
| 16 | グリッド実装 | **TanStack Table v8 + TanStack Virtual**（ヘッドレス・自作、フル装飾） |
| 17 | デザイン | **Tasuki 風（深緑アクセント + クリーム/オフホワイト地）**、shadcn/ui + Tailwind |
| 18 | 条件付きステータス | **ルールビルダー**（条件→ステータス/色 の IF-THEN を複数、上から最初の一致を表示） |
| 19 | 週次グリッドの有無 | **シートごとに ON/OFF**。基本は 1 シートだけ週次グリッドあり、他は属性/集計テーブル（lookup 対象） |
| 20 | ID（キー） | **自動採番を既定 + 手入力許可**。シートごとにキー列を 1 つ指定、シート内重複チェック、接頭辞ルール対応 |
| 21 | v1 スコープ | **全部入りを一気に**（フェーズ分けせず、ただしビルド順は依存関係に従う） |

## 3. データモデル（PostgreSQL）

```
organizations
  id, name, slug, settings JSONB { week_start_weekday: 1(月) }, created_at

users
  id, org_id FK, email(uniq), password_hash, name,
  role ENUM(admin|member), created_at

sheets
  id, org_id FK, name, order,
  has_week_grid BOOL,
  key_column_id FK->columns,          -- ID（キー）列
  color_basis_column_id FK->columns,  -- ガント色の基準列
  numbering_rule JSONB { prefix, digits, next_seq },
  created_at

columns
  id, sheet_id FK, name, order, type, is_key BOOL,
  config JSONB
    -- dropdown:  { options: [{value, color}] }
    -- status:    { rules: [{ conditions:[{col_id, op, value}], label, color }] }  (上から最初の一致)
    -- lookup:    { target_sheet_id, match_key_column_id, return_column_id }
    -- date/text/number/member: 付加設定なし or 軽微

rows
  id, sheet_id FK, key_value,         -- 例: P26-001（uniq within sheet）
  data JSONB { col_id: value },
  version INT,                        -- 楽観ロック
  created_at, created_by, updated_at, updated_by

effort_entries                       -- 週次工数（1セル1行）
  id, row_id FK, week_start DATE,
  planned_hours NUMERIC, actual_hours NUMERIC,
  version INT, updated_at, updated_by
  UNIQUE(row_id, week_start)

row_milestones                       -- 行ごとのフェーズ境界（連続バーの色分け）
  id, row_id FK, name, boundary_date DATE, color, order
  -- バーは boundary_date で区間分割し color で塗る。超過＋未完は遅延色

sheet_snapshots                      -- 変化点用・週次スナップショット
  id, sheet_id FK, for_week DATE,    -- その週初時点の状態
  state JSONB { columns:[...], rows:{ key: { data, ... } } }
  created_at
  UNIQUE(sheet_id, for_week)
```

- 集計・フィルタは JSONB 演算子。よく使う列は式インデックス／生成列で速度担保。
- `member` 型列は `users.id` を参照（担当者）。Tasuki の「マイタスク」相当の "自分担当の行" フィルタを提供。

## 4. 主要機能の挙動

### 4.1 週次工数グリッド（連続ガント）
- 左に属性列を固定、右へ週列（`week_start` 月曜）。**既定は約1年分**を細い列幅で一望（列幅は細/標準/広で調整）。
- 常に**週ごとの数値**を表示・編集（年概要でも数値あり）。
- **工数のある週だけ**連続したフェーズ色で塗る（ゼロ/未入力は無色）。セル個別の枠で区切らずガントらしく見せ、数値はその上に重ねる。
- セル数値は単一表示で時点により意味が変わる: **過去週＝実績h／現在週以降＝予定h**。予定・実績は両方 `effort_entries` に保持。
- 過去の計画は週次スナップショットを **as-of 週**として遡って参照。**基準週ステッパー（‹ ›）で前週時点の計画にワンクリック切替**、「今日に戻る」で復帰（「前の計画が残っていて見返せる」）。
- 固定の属性列（ID/件名/担当/ステータス等の設定列）はどの列幅でも常に表示し、潰さない。
- 今日線を表示。表示ウィンドウは自由に変更可、データは全週永続。

### 4.1b フェーズ／マイルストンと色
- 各行は**マイルストン＝フェーズ境界の日付**（設計→実装→テスト等、名称＋色）を持つ。
- 連続バーは各フェーズ区間ごとにその色で塗り分け。
- 今日が次の境界（マイルストン）を超えても未完なら、超過区間を**遅延色**で表示。
- 完了/遅延の判定は条件付きステータス（ルールビルダー）でマイルストン日付＋完了状態を評価し、それを色基準にする（先の「色基準列」決定をマイルストン版に統合）。

### 4.2 変化点（週次スナップショット差分）
- シートアクセス時、現在週 > 直近スナップショット週なら、**前週初時点の状態を遅延スナップショット**。
- 連続するスナップショット間の差分を計算し、変化があったセルは**数値の文字色を変える**（ドットや背景ではなく）。ホバーで「この週の変化: 行追加 / 日付 4/10→4/17 / 担当者 A→B 等」。
- 注意: 数週間アクセスが無いと中間週がまとめて 1 回の差分に集約される（ライブ状態は再構成不可）。その旨を UI で明示。

### 4.3 条件付きステータス列
- ルールリストを上から評価し、最初に一致した `{label, color}` を表示。色基準列にも使用可。

### 4.4 参照(lookup)列（XLOOKUP）
- `{target_sheet_id, match_key_column_id, return_column_id}`。このシートの照合キー（既定 = キー列）で対象シートを先頭一致検索し、取得列の値をライブ計算で表示（読み取り専用）。未ヒットは空欄。参照値も色基準列・集計に使用可。

### 4.5 ダッシュボード（集計）
- シート単位。指定列でグループ化し、予定/実績合計・件数・消化率をピボット表 + 棒/積上グラフで表示。週範囲で集計。

### 4.6 認証・権限
- 管理者がメンバー作成（初期パスワード手渡し、SMTP 不要）。Cookie セッション。
- Admin: メンバー管理 / シート作成 / 組織設定 / 色・ステータス定義。Member: データ閲覧・編集。全員が全シートを閲覧可。

## 5. 画面（情報設計）
- ログイン
- シート一覧 / 切替
- シート画面（コアグリッド: 属性列 + 週次工数 + ガント色 + 変化点インジケータ + 表示週ウィンドウ）
- ダッシュボード（ピボット + グラフ）
- マイタスク（自分担当の行フィルタ）
- メンバー管理（Admin）
- シート設定（列定義・型・ルールビルダー・色・採番・キー列・色基準列）
- 組織設定 / アカウント設定
- エクスポート（CSV/Excel）

## 6. ビルド順（依存関係順、スコープは全部入り）
1. 足場: FastAPI + Postgres（docker-compose）、Alembic、React+Vite+TS+Tailwind+shadcn、認証スケルトン
2. 認証 + 組織 + メンバー + ロール
3. シート/列 CRUD（text/dropdown/date/number/member）+ 行 CRUD（JSONB）+ キー列・ID 採番 + 楽観ロック
4. コアグリッド UI（TanStack Table + Virtual、固定列 + 週列水平スクロール + セル編集 + 表示週ウィンドウ）
5. 週次工数（予定/実績）+ ガント色 + 凡例
6. status ルールビルダー + lookup 列
7. lookup 解決（XLOOKUP）
8. ダッシュボード（ピボット + グラフ）
9. 変化点（遅延週次スナップショット + 差分 + 週インジケータ）
10. エクスポート（CSV/Excel）
11. 仕上げ: Tasuki 風テーマ、設定画面、マイタスク

## 7. 前提（明示しておく標準値・異議があれば指摘）
- デプロイ: docker-compose（FastAPI + Postgres + nginx で React ビルド配信）。開発は Windows（Docker Desktop もしくは venv + Node）。
- DB マイグレーション: Alembic。
- UI 言語: 日本語、タイムゾーン JST。
- 通知（ベル）/ SMTP メール送信: v1 スコープ外。
- バックアップ・運用監視: アプリ機能ではなくインフラ側の課題として別途。

## 8. 未確定／要確認（次セッションで詰める候補）
- グリッドの行数・シート数・想定週数の規模感（仮想化チューニングと JSONB インデックス方針）。
- 採番ルールが行カテゴリ（P/E など接頭辞違い）でどう分岐するか。
- ダッシュボードのグラフ種別の優先度（積上棒 / 折線 / ヒートマップ）。
- エクスポートの対象（生データ / ガント見た目 / 集計）と形式（.xlsx / .csv）。
- フェーズ「完了/遅延」の判定根拠（手動完了フラグ / 実績h が予定h に到達 / ステータス列）。
